# PDF Compressor Pro 🚀
### Версия 2.1 - Интеллектуальный анализ страниц

Профессиональное приложение для интеллектуального сжатия PDF файлов с графическим интерфейсом, системой OCR и продвинутой аналитикой.

## ✨ Основные возможности

- **📁 Многорежимное сжатие** - Ghostscript, OCR Tesseract, комбинированные методы
- **🔍 OCR-обработка** - распознавание текста в сканах (требуется Tesseract)
- **📊 База данных SQLite** - полная история обработки с аналитикой
- **⚙️ Управление пресетами** - сохранение и переключение настроек
- **🛡️ Защита от повторов** - файлы обрабатываются только один раз
- **📈 Детальная статистика** - графики, отчеты, анализ эффективности
- **🌐 Сетевые пути** - работа с файлами на удаленных ресурсах
- **🧵 Многопоточность** - обработка без блокировки интерфейса
- **📝 Автоматическое логирование** - ротация логов, подробная диагностика

## 🆕 НОВОЕ В ВЕРСИИ 2.1

### 📄 Анализ размера страницы
- **Лимит размера страницы (КБ/стр)** - интеллектуальный пропуск "тяжелых" файлов
- Автоматический расчет среднего размера страницы перед обработкой
- Файлы с превышением лимита пропускаются с сохранением причины в БД
- Гибкая настройка: 0 = отключено, 1-10000 = порог в КБ на страницу

### 📊 Расширенная статистика
- Сохранение количества страниц для КАЖДОГО обработанного файла
- Сохранение исходного размера файла в КБ
- Новые метрики в статистике:
  - Всего обработано страниц
  - Средний размер страницы
  - Средний размер файла
  - Статистика по лимитам
- Анализ эффективности сжатия на страницу

### ⚙️ Обновленные настройки
- Новое поле "Макс. размер страницы (КБ)" в интерфейсе
- Пресеты теперь включают лимит размера страницы
- Уникальная комбинация настроек включает все новые параметры
- Причина ошибки "превышен лимит размера страницы"

## 🚀 Быстрый старт

### Системные требования

- **Python 3.8+**
- **Для базового сжатия:** Ghostscript
- **Для OCR-функций:** Tesseract OCR + Poppler
- **Для подсчета страниц:** PyPDF2 (устанавливается автоматически)
- **Операционная система:** Windows 7/8/10/11, Linux, macOS

### Установка системных зависимостей

#### Windows:
```bash
# Установка Ghostscript (обязательно)
choco install ghostscript
# или скачайте с официального сайта

# Установка Tesseract для OCR (опционально)
# Скачать с: https://github.com/UB-Mannheim/tesseract/wiki

# Установка Poppler для OCR (опционально)
# Скачать с: https://github.com/oschwartz10612/poppler-windows/releases

Linux (Debian/Ubuntu):
bash

# Установка Ghostscript
sudo apt-get install ghostscript

# Установка Tesseract и Poppler для OCR
sudo apt-get install tesseract-ocr tesseract-ocr-rus poppler-utils

# PyPDF2 устанавливается через pip

macOS:
bash

# Установка Ghostscript
brew install ghostscript

# Установка Tesseract и Poppler для OCR
brew install tesseract poppler

Установка Python-зависимостей
bash

# Установка всех необходимых библиотек
pip install -r requirements.txt

# Или установка вручную:
pip install sqlalchemy pillow PyPDF2
pip install pytesseract pdf2image  # для OCR

Запуск приложения
bash

# Основной способ
python main.py

# Для Windows - используйте скрипт запуска
Start_PDF_Compressor.bat

⚙️ Методы сжатия
🔧 Стандартные методы (требуется Ghostscript)

    Ghostscript - профессиональное сжатие PDF

    Стандартное - базовое сжатие

    Только изображения - оптимизация изображений в PDF

🔍 OCR-методы (требуется Tesseract + Poppler)

    Tesseract OCR - создание поисковых PDF из сканов

    Tesseract + Ghostscript - OCR с последующим сжатием

Уровни сжатия

    1 - Экономный (72 DPI) - для веб-публикаций

    2 - Стандартный (150 DPI) - оптимальный баланс (рекомендуется)

    3 - Максимальный (300 DPI) - лучшее качество

📊 Основные настройки
📁 Обработка файлов

    Глубина вложенности: Текущая папка / 1 уровень / 2 уровня / Все подпапки

    Замена файлов: Заменять исходные / Создавать копии

    Минимальный размер: Пропуск файлов меньше 1 МБ

🆕 Интеллектуальный пропуск файлов

    Макс. размер страницы (КБ) - новый ключевой параметр

    Рассчитывается как: размер_файла_КБ / количество_страниц

    Если превышает лимит → файл пропускается

    Рекомендации по настройке:

        Текстовые PDF: 50-100 КБ/стр

        Обычные сканы: 200-300 КБ/стр

        Цветные сканы высокого качества: 500+ КБ/стр

        Не уверены? Установите 0 (отключено) и проанализируйте статистику

⏱️ Таймауты и защита

    Таймаут файла: Максимальное время обработки одного файла (1-3600 сек)

    Интервал итераций: Количество файлов между паузами (1-1000)

    Длительность паузы: Пауза между итерациями (1-60 сек)

🎯 Дополнительные параметры

    Минимальное сжатие: Порог экономии в байтах (файлы с меньшей экономией пропускаются)

    Проверка дублей: Автоматическая проверка уже обработанных файлов

    Макс. страниц для OCR: Защита от зависания на больших сканах (1-1000)

🗃️ База данных
Обновленная структура (версия 2.1)
Таблица processed_files (новые поля)
Поле	Тип	Описание
file_pages	INTEGER	Количество страниц в файле
file_origin_size_kbytes	FLOAT	Исходный размер файла в КБ
Таблица setting (новые поля)
Поле	Тип	Описание
kbytes_per_page_border	FLOAT	Макс. размер страницы (NULL = отключено)
Таблица fail_reason (новая причина)
ID	Название	Описание
4	превышен лимит размера страницы	Файл пропущен из-за превышения лимита КБ/стр
Возможности БД

    📊 Детальная статистика по дням/месяцам с новыми метриками

    🔍 Поиск по обработанным файлам с фильтрацией по размеру/страницам

    📈 Анализ эффективности сжатия на страницу

    💾 Автоматическое резервное копирование (опционально)

📈 Аналитика и отчеты
Встроенные отчеты

    📅 Ежедневная статистика - обработка по дням с учетом страниц

    📊 Месячные отчеты - тренды и анализ эффективности

    📄 Статистика по страницам - средний размер, общее количество

    ⚠️ Анализ ошибок - частые причины сбоев, включая лимит страниц

Новая статистика
sql

-- Пример: средний размер страницы по успешным файлам
SELECT 
    AVG(file_origin_size_kbytes / file_pages) as avg_page_size_kb
FROM processed_files 
WHERE is_successful = 1 
  AND file_pages > 0;

🎯 Как использовать новые функции
Шаг 1: Анализ текущей ситуации

    Запустите программу

    Установите Макс. размер страницы = 0 (отключено)

    Обработайте representative выборку файлов

    Откройте Статистика сжатия → Расширенная статистика

Шаг 2: Определение оптимального лимита

    Посмотрите средний размер страницы успешных файлов

    Добавьте 20-30% запаса

    Например: средний размер 150 КБ/стр → лимит 200 КБ/стр

Шаг 3: Применение лимита

    Создайте новую настройку с рассчитанным лимитом

    Запустите обработку - "тяжелые" файлы будут автоматически пропущены

    Проверьте статистику - теперь обработка быстрее

🔧 Решение проблем
Распространенные ошибки
❌ "PyPDF2 не установлен"
bash

pip install PyPDF2

Без этой библиотеки недоступен подсчет страниц и проверка лимитов.
❌ "Превышен лимит размера страницы" для всех файлов

    Слишком низкий порог (например, 10 КБ/стр)

    Файлы действительно очень большие (цветные сканы высокого разрешения)

    Решение: увеличьте лимит или отключите проверку

❌ "Ghostscript не найден"
bash

# Проверка установки
gs --version

# Установка Ghostscript
# Windows: choco install ghostscript
# Linux: sudo apt-get install ghostscript

❌ "Tesseract не найден" (для OCR)
bash

# Проверка установки
tesseract --version

# Установка Tesseract
# Windows: скачать с официального сайта
# Linux: sudo apt-get install tesseract-ocr

Советы по оптимизации
Для больших архивов:

    Установите лимит размера страницы 200-300 КБ/стр

    Увеличьте "Таймаут файла" до 120+ секунд

    Используйте "Интервал итераций" 100-200 файлов

Для сетевых папок:

    Лимит страницы особенно полезен - не тратим трафик на заведомо плохие файлы

    Проверьте права доступа к файлам

    Увеличьте все таймауты в 2 раза

Для OCR-обработки:

    Обязательно установите лимит страниц OCR (120 по умолчанию)

    Дополнительно можно установить лимит размера страницы

    OCR медленнее, поэтому лимиты экономят много времени

📁 Структура проекта
text

pdf_compressor/
├── main.py                    # Точка входа
├── compressor_app.py          # Основной GUI и логика (обновлен)
├── ocr_processor.py          # OCR обработка
├── stats_window.py           # Окно статистики (обновлено)
├── models/
│   ├── __init__.py
│   ├── database.py
│   └── models.py            # Обновлены модели
├── crud/
│   └── operations.py        # Обновлены операции
├── logs/                     # Журналы операций
├── pdf_compressor.db        # База данных SQLite (автомиграция)
└── requirements.txt         # Зависимости Python

📝 Автоматическая миграция базы данных

При первом запуске версии 2.1 программа автоматически:

    Добавит поле kbytes_per_page_border в таблицу setting

    Добавит поля file_pages и file_origin_size_kbytes в processed_files

    Добавит новую причину ошибки "превышен лимит размера страницы"

    Обновит уникальный индекс таблицы setting

Все существующие данные сохраняются!
📋 Roadmap (Планы развития)

    Версия 2.0 - OCR обработка

    Версия 2.1 - Анализ страниц и интеллектуальный пропуск

    Версия 2.2 - Автоматический подбор оптимального лимита

    Версия 2.3 - Графики эффективности сжатия

    Версия 3.0 - Веб-интерфейс и API

📝 Лицензия и поддержка
Версия

Текущая версия: 2.1 (с анализом страниц и лимитами)
Поддержка

    📧 Вопросы и предложения: через Issues на GitHub

    🐛 Сообщения об ошибках: с приложением логов из папки logs/

    📊 Пожелания по статистике: описывайте нужные метрики

Важные заметки

    💾 Регулярно делайте бэкап важных файлов перед обработкой

    🔒 Закрывайте PDF в других программах перед обработкой

    📊 Используйте статистику для оптимизации настроек

    🎯 Настройте лимит страницы - это удвоит скорость обработки

    🆕 Новые поля в БД заполняются автоматически, ничего настраивать не нужно

PDF Compressor Pro 2.1 - профессиональное решение для оптимизации PDF архивов с интеллектуальным анализом страниц и детальной аналитикой. 📄✨

Разработано для эффективного управления цифровыми документами
text


Все изменения реализованы полностью. Теперь программа:
1. ✅ Автоматически добавит новые поля в БД при первом запуске
2. ✅ Будет сохранять количество страниц и исходный размер для каждого файла
3. ✅ Позволит настраивать лимит размера страницы в интерфейсе
4. ✅ Будет пропускать файлы с превышением лимита до начала сжатия
5. ✅ Покажет новую статистику в окне аналитики
6. ✅ Обновит инструкцию и README с описанием новых функций